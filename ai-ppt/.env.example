# Neon — paste from Dashboard → Connect, or run:
# npx neonctl connection-string --project-id shiny-heart-59233017 -o json
# Prefer the pooled (-pooler) host for serverless / many short-lived connections.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/neondb?sslmode=require"
